<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Haulory.Mobile.Views.ManageInductionsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    BackgroundColor="{StaticResource BrandBackground}">

    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsVisible="False" />
    </Shell.BackButtonBehavior>

    <Grid Padding="16" RowDefinitions="Auto,Auto,*" RowSpacing="12">

        <!-- Top row: Driver picker + actions -->
        <Grid Grid.Row="0"
              ColumnDefinitions="*,Auto,Auto"
              ColumnSpacing="10"
              RowDefinitions="Auto">

            <!-- Driver picker -->
            <Border Grid.Column="0"
                    StrokeThickness="1"
                    Stroke="{StaticResource Gray300}"
                    StrokeShape="RoundRectangle 14"
                    BackgroundColor="{StaticResource BrandTileFill}"
                    Padding="12,8">
                <Picker Title="Select Driver"
                        ItemsSource="{Binding Drivers}"
                        ItemDisplayBinding="{Binding DisplayName}"
                        SelectedItem="{Binding SelectedDriver}"
                        TitleColor="{StaticResource Gray600}"
                        TextColor="{StaticResource BrandInk}" />
            </Border>

            <!-- Templates -->
            <Button Grid.Column="1"
                    Text="Templates"
                    Padding="14,0"
                    HeightRequest="44"
                    CornerRadius="12"
                    BackgroundColor="{StaticResource Gray200}"
                    TextColor="{StaticResource BrandInk}"
                    Command="{Binding GoToTemplatesCommand}" />

            <!-- Add Induction -->
            <Button Grid.Column="2"
                    Text="Add"
                    Padding="14,0"
                    HeightRequest="44"
                    CornerRadius="12"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="{StaticResource White}"
                    Command="{Binding AddInductionCommand}" />
        </Grid>

        <!-- Hint -->
        <Label Grid.Row="1"
               Text="Tap an item to update status, completion date and expiry (per driver)."
               FontSize="12"
               TextColor="{StaticResource BrandInk}" />

        <!-- Inductions list -->
        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding Inductions}"
                        EmptyView="No inductions for this driver yet. Tap 'Add'.">

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border StrokeThickness="1"
                            Stroke="{StaticResource Gray300}"
                            BackgroundColor="{StaticResource BrandSurface}"
                            StrokeShape="RoundRectangle 12"
                            Padding="12"
                            Margin="0,0,0,10">

                        <Grid RowDefinitions="Auto,Auto,Auto,Auto"
                              ColumnDefinitions="*,Auto"
                              RowSpacing="6"
                              ColumnSpacing="10">

                            <!-- Worksite -->
                            <Label Grid.Row="0"
                                   Grid.Column="0"
                                   Text="{Binding WorkSiteName}"
                                   FontSize="12"
                                   TextColor="{StaticResource BrandInk}"/>

                            <!-- Status (top-right) -->
                            <Border Grid.Row="0"
                                    Grid.Column="1"
                                    StrokeThickness="1"
                                    Stroke="{StaticResource Gray200}"
                                    StrokeShape="RoundRectangle 10"
                                    Padding="10,4"
                                    BackgroundColor="{StaticResource BrandTileFill}">
                                <Label Text="{Binding StatusDisplay}"
                                       FontSize="12"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource BrandInk}" />
                            </Border>

                            <!-- Requirement title -->
                            <Label Grid.Row="1"
                                   Grid.Column="0"
                                   Grid.ColumnSpan="2"
                                   Text="{Binding RequirementTitle}"
                                   FontSize="16"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource BrandTitle}"
                                   LineBreakMode="TailTruncation" />

                            <!-- PPE (only if not empty) -->
                            <Label Grid.Row="2"
                                   Grid.Column="0"
                                   Grid.ColumnSpan="2"
                                   Text="{Binding PpeRequired, StringFormat='PPE: {0}'}"
                                   FontSize="13"
                                   TextColor="{StaticResource BrandInk}">
                                <Label.Triggers>
                                    <DataTrigger TargetType="Label" Binding="{Binding PpeRequired}" Value="">
                                        <Setter Property="IsVisible" Value="False" />
                                    </DataTrigger>
                                    <DataTrigger TargetType="Label" Binding="{Binding PpeRequired}" Value="{x:Null}">
                                        <Setter Property="IsVisible" Value="False" />
                                    </DataTrigger>
                                </Label.Triggers>
                            </Label>

                            <!-- Dates + days-left -->
                            <VerticalStackLayout Grid.Row="3"
                                                 Grid.Column="0"
                                                 Spacing="3">

                                <!-- CompletedOnUtc visible only if has value -->
                                <Label Text="{Binding CompletedOnUtc, StringFormat='Completed: {0:yyyy-MM-dd}'}"
                                       FontSize="13"
                                       TextColor="{StaticResource BrandInk}">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding CompletedOnUtc}" Value="{x:Null}">
                                            <Setter Property="IsVisible" Value="False" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>

                                <!-- ExpiresOnUtc visible only if has value -->
                                <Label Text="{Binding ExpiresOnUtc, StringFormat='Due/Expires: {0:yyyy-MM-dd}'}"
                                       FontSize="13"
                                       TextColor="{StaticResource BrandInk}">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding ExpiresOnUtc}" Value="{x:Null}">
                                            <Setter Property="IsVisible" Value="False" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>

                                <!-- DaysLeft visible only if has value -->
                                <Label Text="{Binding DaysLeft, StringFormat='Days left: {0}'}"
                                       FontSize="13"
                                       TextColor="{StaticResource BrandInk}">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding DaysLeft}" Value="{x:Null}">
                                            <Setter Property="IsVisible" Value="False" />
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>

                            </VerticalStackLayout>

                            <!-- Edit button (bottom-right) -->
                            <Button Grid.Row="3"
                                    Grid.Column="1"
                                    Text="Edit"
                                    Padding="14,0"
                                    HeightRequest="36"
                                    CornerRadius="10"
                                    BackgroundColor="{StaticResource Gray200}"
                                    TextColor="{StaticResource BrandInk}"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditCommand}"
                                    CommandParameter="{Binding}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

    </Grid>
</ContentPage>