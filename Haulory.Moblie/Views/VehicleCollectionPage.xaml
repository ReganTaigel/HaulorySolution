<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Haulory.Mobile.Views.VehicleCollectionPage"
             Title="Vehicles">

    <Grid RowDefinitions="Auto,*"
          Padding="20"
          RowSpacing="14">

        <!-- Header -->
        <Grid Row="0" ColumnDefinitions="*,Auto">
            <Label Text="Your Vehicles"
                   FontSize="18"
                   FontAttributes="Bold"
                   VerticalOptions="Center" />

            <Button Grid.Column="1"
                    Text="Add"
                    Command="{Binding GoToNewVehicleCommand}"
                    VerticalOptions="Center"/>
        </Grid>

        <!-- List + Pull to refresh -->
        <RefreshView Grid.Row="1"
                     Command="{Binding RefreshCommand}"
                     IsRefreshing="{Binding IsBusy}">

            <CollectionView ItemsSource="{Binding Assets}"
                            SelectionMode="None">

                <CollectionView.EmptyView>
                    <VerticalStackLayout Padding="10" Spacing="8">
                        <Label Text="No vehicles saved yet."
                               FontAttributes="Bold"
                               FontSize="16" />
                        <Label Text="Tap Add to create your first vehicle."
                               FontSize="12"
                               TextColor="Gray" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeThickness="1"
                                Stroke="{AppThemeBinding Light='#E5E7EB', Dark='#374151'}"
                                StrokeShape="RoundRectangle 16"
                                BackgroundColor="{AppThemeBinding Light='#FFFFFF', Dark='#111827'}"
                                Padding="14"
                                Margin="0,0,0,12">

                            <VerticalStackLayout Spacing="8">

                                <!-- TITLE -->
                                <Label FontSize="16" FontAttributes="Bold">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="{Binding Year}" />
                                            <Span Text=" " />
                                            <Span Text="{Binding Make}" />
                                            <Span Text=" " />
                                            <Span Text="{Binding Model}" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <!-- TYPE LINE -->
                                <HorizontalStackLayout Spacing="8">
                                    <Label Text="{Binding VehicleTypeDisplay}"
                                           FontSize="12"
                                           TextColor="Gray"
                                           VerticalOptions="Center" />
                                </HorizontalStackLayout>

                                <!-- REGO -->
                                <Label FontSize="12" TextColor="Gray">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Rego: " />
                                            <Span Text="{Binding Rego}" FontAttributes="Bold" />
                                            <Span Text=" • Expires: " />
                                            <Span Text="{Binding RegoExpiryDisplay}" FontAttributes="Bold" />

                                            <!-- Status (auto hides bullet when empty) -->
                                            <Span Text="{Binding RegoStatusInlineDisplay}" FontAttributes="Bold" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <!-- WOF / COF -->
                                <Label FontSize="12" TextColor="Gray">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="{Binding CertificateNameDisplay}" />
                                            <Span Text=" expiry: " />
                                            <Span Text="{Binding CertificateExpiryDisplay}" FontAttributes="Bold" />

                                            <!-- Status (auto hides bullet when empty) -->
                                            <Span Text="{Binding CertStatusInlineDisplay}" FontAttributes="Bold" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>

                                <!-- RUC (Licence range) -->
                                <VerticalStackLayout IsVisible="{Binding IsRucApplicable}" Spacing="2">

                                    <Label FontSize="12" TextColor="Gray">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="RUC licence: " />
                                                <Span Text="{Binding RucLicenceStartDisplay}" FontAttributes="Bold" />
                                                <Span Text=" - " />
                                                <Span Text="{Binding RucLicenceEndDisplay}" FontAttributes="Bold" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>

                                    <Label FontSize="12" TextColor="Gray">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Remaining: " />
                                                <Span Text="{Binding RucRemainingDisplay}" FontAttributes="Bold" />
                                                <Span Text=" • Overdue: " />
                                                <Span Text="{Binding IsRucOverdueYesNo}" FontAttributes="Bold" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>

                                </VerticalStackLayout>


                            </VerticalStackLayout>

                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>
        </RefreshView>

    </Grid>
</ContentPage>
